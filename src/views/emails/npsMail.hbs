  <style>
    .container {
      display: flex;
      align-content: center;
      align-items: center;
      justify-content: center;
      flex-direction: column;
    }

    .title {
      margin: 20px;
    }

    .description {
      margin-bottom: 20px;
    }

    .level {
      display: flex;
      margin: 10px;
      justify-content: space-between;
      width: 400px;
    }

    .answers {
      display: flex;
      width: 400px;
      justify-content: space-between;
      margin-bottom: 20px;
    }

    .value {
      padding: 10px;
      background: chartreuse;
      color: #fff;
      border-radius: 6px;
      width: 15px;
      text-align: center;
      text-decoration: none;
    }
  </style>

  <div class="container">
    <label>Olá <strong>{{name}}</strong>! Tudo bem?</label>

    <h3 class="title">{{title}}</h3>
    <strong class="description">{{description}}</strong>

    <div class="level">
      <span>Pouco provável</span>
      <span>Muito provável</span>
    </div>

    <div class="answers">
      <a class="value" href="{{link}}/1?u={{user_id}}">1</a>
      <a class="value" href="{{link}}/2?u={{user_id}}">2</a>
      <a class="value" href="{{link}}/3?u={{user_id}}">3</a>
      <a class="value" href="{{link}}/4?u={{user_id}}">4</a>
      <a class="value" href="{{link}}/5?u={{user_id}}">5</a>
      <a class="value" href="{{link}}/6?u={{user_id}}">6</a>
      <a class="value" href="{{link}}/7?u={{user_id}}">7</a>
      <a class="value" href="{{link}}/8?u={{user_id}}">8</a>
      <a class="value" href="{{link}}/9?u={{user_id}}">9</a>
      <a class="value" href="{{link}}/10?u={{user_id}}">10</a>
    </div>

    <strong>Sua opinição é muito importante para nós</strong>
    <h3>Equipe | <strong>Solid Tecnologias</strong></h3>
  </div>
